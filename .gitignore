# secrets
.env
client_secret.json

# environments / caches
venv/
__pycache__/
*.pyc

# runtime data (clips, renders, caches, tokens)
data/

# training datasets (large; regenerable via twitch_clips collect mode)
twitch_clips/data/

# generated music (rebuild with: python -m pipeline.gen_music)
assets/music/bg.mp3

# archived pre-pivot code (local only)
_archive/

# local Claude Code permissions (machine-specific)
.claude/settings.local.json

# one-off batch scripts (not part of the distributed setup)
run_days.bat