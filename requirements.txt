# Core pipeline
requests
pyyaml
yt-dlp
edge-tts
tzdata

# Pre-filter scoring (audio hype / motion)
librosa
numpy
opencv-python

# Thumbnail compositing
Pillow

# YouTube upload (stage 10)
google-api-python-client
google-auth-oauthlib

# Multilingual speech -> English (enrichment/transcribe.py: commentary context + shorts
# captions). GPU-accelerated on the RTX 3050 (int8_float16) when CUDA is present, else CPU.
faster-whisper

# ffmpeg + ffprobe must be on PATH
# Local VLM filtering needs Ollama (https://ollama.com) with a vision model,
# e.g.:  ollama pull qwen3-vl:4b
