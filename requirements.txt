# Kept for `pip install -r requirements.txt` compatibility.
# The authoritative dependency list is pyproject.toml — prefer:
#
#   pip install -e .                 # core: fetch, filter, render
#   pip install -e ".[all]"          # + transcription, voiceover, upload
#   pip install -e ".[transcribe]"   # pick individual extras
#
# ffmpeg and ffprobe must be on PATH (not installable via pip).
# Local VLM filtering needs Ollama (https://ollama.com) with a vision model:
#   ollama pull qwen3-vl:4b
#
# Run `python -m pipeline.doctor` to check everything at once.

-e .
